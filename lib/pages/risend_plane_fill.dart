import 'package:flutter/material.dart';

class RisendPlaneFill extends StatelessWidget {
  const RisendPlaneFill({super.key});

  @override
  Widget build(BuildContext context) {
  // ignore: unused_local_variable
  double fem = MediaQuery.of(context).size.width / 24;
    return 
    Container(
    );
  }
}